---
apiVersion: aws.m.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
  namespace: database-team
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: database-team
      name: aws-creds
      key: credentials
